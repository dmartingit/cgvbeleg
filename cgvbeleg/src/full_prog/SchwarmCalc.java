package full_prog;

import math.*;



public class SchwarmCalc  {
/*	public Vektor2D seek(Vektor2D target) {
		Vektor2D help;
		help.differenz(target, meinFahrzeug.getPosition());
		help.normalize();
		help.mult(meinFahrzeug.getMaxSpeed());
		help.sub(meinFahrzeug.getVelocity());
		return help;
		}
*/
}
